# Developer Documentation

Since `docs/` is reserved for `docs.noports.com` content, `dev_docs/` serves as
the home for content which should be documented for development and architectural
reasons, but shouldn't be included in the documentation site.

