  image-runtime-release:
    deploy:
      mode: replicated
      replicas: 0
    build:
      context: ../image/
      dockerfile: ./Dockerfile
      target: runtime-release
      args:
        # auto added:
        # - release
    # auto added:
    # - image
