  image-runtime-release:
    deploy:
      mode: replicated
      replicas: 0
    build:
      context: ../image/
      dockerfile: ./Dockerfile
      target: runtime-release
      args:
        # auto added:
        # - branch
    # auto added:
    # - image
