  container-sshnp:
    container_name: sshnp
    volumes:
      - ../contexts/sshnp/:/atsign/
    networks:
      - sshnp
    # auto added:
    # - image
    # - depends_on: (sshnpd + runtime service)
