import 'package:flutter/material.dart';

class PolicyManagerView extends StatelessWidget {
  final String? atSign;
  const PolicyManagerView({super.key, this.atSign});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}