import 'package:flutter/material.dart';

class ProfileBarStats extends StatelessWidget {
  const ProfileBarStats({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 0,
      height: 0,
    );
  }
}
